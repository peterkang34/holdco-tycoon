# Existing services
ADMIN_SECRET="your-admin-secret"
ANTHROPIC_API_KEY="sk-ant-..."
KV_REST_API_READ_ONLY_TOKEN="..."
KV_REST_API_TOKEN="..."
KV_REST_API_URL="https://your-project.upstash.io"
KV_URL="rediss://..."
REDIS_URL="rediss://..."

# Supabase — Player Accounts (Phase 1)
# Client-side (VITE_ prefix = bundled into client, safe to expose — RLS enforces access)
VITE_SUPABASE_URL="https://your-project.supabase.co"
VITE_SUPABASE_ANON_KEY="your-anon-key"
# Server-side only (bypasses RLS — NEVER prefix with VITE_)
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"
